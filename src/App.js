import { BrowserRouter } from 'react-router-dom';
import { Routes } from './Routes';

const App = () => {
  return (
    <BrowserRouter>
      <Routes></Routes>
    </BrowserRouter>
  );
};

export default App;
